[package]
name = "polar-landing"
version = "0.1.0"
edition = "2021"
description = "Rust/WebAssembly landing page for a polar exploration & Arctic science portfolio"

[dependencies]
yew = { version = "0.21", features = ["csr"] }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
